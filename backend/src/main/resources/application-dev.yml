spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    locations: classpath:db/migration

logging:
  level:
    com.st6.weekly: DEBUG
    org.hibernate.SQL: DEBUG
